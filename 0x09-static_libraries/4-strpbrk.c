#include "main.h"
#include <string.h>

/**
 * _strpbrk - Entry
 * @s: string
 * @accept: string
 *
 * Return: strpbrk if success otherwise if fail
 */
char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
