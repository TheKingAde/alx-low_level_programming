#include "main.h"
#include <string.h>

/**
 * _strchr - Entry
 * @s: string
 * @c: character
 *
 * Return: strchr if success otherwise if fail
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
