#include "main.h"

/**
 * mul - writes the character c to stdout
 * @a: number
 * @b: number
 *
 * Return: On success computed number.
 * On error, -1 is returned
 */
int mul(int a, int b)
{
	return (a * b);
}
