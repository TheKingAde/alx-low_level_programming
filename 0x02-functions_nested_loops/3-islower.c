#include "main.h"

/**
 * _islower - Entry point
 *@c: The character to be checked
 *
 * Return: 1 if lowercase and 0 if otherwise (Success)
 */

int _islower(int c)
{
	int result;

	if (_islower(c))
	{
		_putchar('1');
		result = 1;
	} else
	{
		_putchar('0');
		result = 0;
	}
	return (result);
}
